class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        answer = []
        sums = 0
        for number in nums:
            sums += number
            answer.append(sums)
            
        return answer
