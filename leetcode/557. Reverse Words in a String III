class Solution:
    def reverseWords(self, s: str) -> str:
        answer = ""
        s = s.split(' ')
        for word in s:
            answer += word[::-1]
            answer += " "
            
        return answer[:-1]
