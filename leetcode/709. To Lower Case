class Solution:
    def toLowerCase(self, s: str) -> str:
        answer = []
        for char in s:
            if ord(char) < ord('a'):
                char = char.lower()
            answer.append(char)
            
        return ''.join(answer)
    
    def toLowerCase2(self, s: str) -> str:
        return s.lower()
